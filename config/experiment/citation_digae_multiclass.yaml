# @package _global_

defaults:
  - citation_gae_baseline
  - override /model: mcdigae

training_objective: multiclass
trainer:
  num_epochs: 80
  lr: 0.001
  log_every_n_steps: 1
  print_every_n_steps: 1